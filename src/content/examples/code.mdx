---
title: code
---

code-fences to syntax highlight:

```ts
const x = 1;
```

inline code

`test`

code-fences to diagram:

```mermaid
flowchart LR
    id
```

code-fences extra:

```some thing else
extra
```
